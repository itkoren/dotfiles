# ╭──────────────────────────────────────────────────────────╮
# │ APPS                                                     │
# ╰──────────────────────────────────────────────────────────╯

---
apps:
  darwin:
    # ── Universal (get installed before the other apps) ──────────────
    universal:
        encrypted:
            - "apps/encrypted.Supercharge.app"
            - "apps/encrypted.PastePal.app"
        plain:
    personal:
        encrypted:
            - "apps/encrypted.Tab Finder.app"
        plain:
    work:
        encrypted:
        plain:
        
