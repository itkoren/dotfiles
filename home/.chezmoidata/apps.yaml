# ╭──────────────────────────────────────────────────────────╮
# │ APPS                                                 │
# ╰──────────────────────────────────────────────────────────╯

---
apps:
  darwin:
    # ── Universal (get installed before the other apps) ──────────────
    universal:
        encrypted:
            - {{- .chezmoi.sourceDir | quote -}}/apps/encrypted.Supercharge.app
        plain:
    personal:
        encrypted:
        plain:
    work:
        encrypted:
        plain:
        
